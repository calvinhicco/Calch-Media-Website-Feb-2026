import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Calch Media - The Touch of Art in Marketing Solutions, Media Production & I.T. Solutions",
  description:
    "Integrated marketing communications, media production, and I.T. solutions company delivering marketing solutions, media production, and powerful digital systems since 2017.",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className} suppressHydrationWarning>
        {children}
      </body>
    </html>
  )
}
